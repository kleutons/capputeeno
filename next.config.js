/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    output: 'export',
    compiler: {
        styledComponents: true
    }
  }
  
  module.exports = nextConfig