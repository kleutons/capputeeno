/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    compiler: {
      styledComponents: true
    },
    output: "export"
  };
  
  module.exports = nextConfig;